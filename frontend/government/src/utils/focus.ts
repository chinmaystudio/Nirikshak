/** Composite focus-visible ring utility for interactive elements. */
export const nkFocus = 'outline-none focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary'

/** Pressable control base shared by Button / IconButton / menu items. */
export const nkControlBase = 'inline-flex items-center justify-center gap-2 rounded-control font-button transition-colors duration-fast disabled:cursor-not-allowed disabled:opacity-50 outline-none focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary'
